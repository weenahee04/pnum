# === Database ===
# Local: DATABASE_URL="file:./dev.db"
# Production (Neon): DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
DIRECT_URL="postgresql://user:pass@host/db?sslmode=require"

# === NextAuth ===
NEXTAUTH_SECRET="generate-a-random-secret-at-least-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# === LINE Notify (Optional) ===
# LINE_NOTIFY_TOKEN="your-line-notify-token"

# === SerpAPI for SEO Keyword Tracking (Optional) ===
# SERPAPI_KEY="your-serpapi-key"
